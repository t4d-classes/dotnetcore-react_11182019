Exercise 5

1. Create a new component named Tool Header. Move the header content from Car Tool to the new component. Pass the header text as props to the Tool Header.

2. Replace the original header content in Car Tool with the new Tool Header component.

3. Create a new component named Car Table. Move the car table content from Car Tool to the new component.

4. Replace the car table content in the Car Tool component, with the new Car Table component.

5. Create a new component named Car View Row. Display a single row of car data in Car View Row.

6. Replace the car table row content in Car Table component with the new Car View Row component.

7. Ensure it works.